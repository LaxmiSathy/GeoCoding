- args:
    sql: CREATE TABLE "users"("user_id" serial NOT NULL, "username" text NOT NULL,
      "group_id" integer , PRIMARY KEY ("user_id") )
  type: run_sql
- args:
    name: users
  type: add_existing_table_or_view
